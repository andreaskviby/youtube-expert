name = "adventure-in-sicily"
compatibility_date = "2024-01-01"
pages_build_output_dir = "public"

# Note: Cloudflare Pages does not support a [build] section in wrangler.toml.
# The GitHub Actions workflow runs `python3 adventure-website/build.py` before
# `wrangler pages deploy`, so no in-config build command is needed here.
